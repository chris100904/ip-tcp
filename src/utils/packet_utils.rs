pub fn encapsulate_packet(packet: &Packet) -> Vec<u8> {
    // Encapsulate the packet for transmission
}

pub fn decapsulate_packet(raw_data: &[u8]) -> Result<Packet, Error> {
    // Decapsulate raw data into a Packet struct
}
